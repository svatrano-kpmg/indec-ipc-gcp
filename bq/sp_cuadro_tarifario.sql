-- Stub para sp_merge_ft_ajustes
CREATE OR REPLACE PROCEDURE `tgs-sandbox.ds_datos_tableros.sp_merge_ft_ajustes`()
BEGIN
  SELECT "Ejecutando lógica de ft_ajustes...";
END;

-- Stub para sp_merge_ft_marcha_calculo
CREATE OR REPLACE PROCEDURE `tgs-sandbox.ds_datos_tableros.sp_merge_ft_marcha_calculo`()
BEGIN
  SELECT "Ejecutando lógica de ft_marcha_calculo...";
END;

-- Stub para sp_merge_ft_cuadro_tarifario
CREATE OR REPLACE PROCEDURE `tgs-sandbox.ds_datos_tableros.sp_merge_ft_cuadro_tarifario`()
BEGIN
  SELECT "Ejecutando lógica de ft_cuadro_tarifario...";
END;